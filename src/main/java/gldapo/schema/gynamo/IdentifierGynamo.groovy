package gldapo.gynamo;
import gynamo.Gynamo

class IdentifierGynamo extends Gynamo
{
	static getIdentifier = { ->
		// TODO, introspect for an attribute with a certain annotation that marks it as the identifier
	}
}